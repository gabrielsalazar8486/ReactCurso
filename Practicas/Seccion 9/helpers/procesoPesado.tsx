const procesoPesado = (interaciones:number) =>{
  for(let i=0; i < interaciones; i++)
  {
    console.log('Ahí vamos....')
  }

  return `${interaciones} interaciones realizadas.`
}
export default procesoPesado